---
- name: Simple become test
  hosts: all

  tasks:
    - name: Check whoami with become
      become: yes
      become_user: root
      command: whoami
      register: whoami_output

    - name: Debug whoami output
      debug:
        var: whoami_output
